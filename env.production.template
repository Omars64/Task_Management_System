# WorkHub Production Environment Variables Template
# Copy this file to .env.production and fill in your values
# DO NOT commit the actual .env.production file to version control

# =============================================================================
# GCP Configuration
# =============================================================================
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
CLOUD_SQL_CONNECTION_NAME=your-project:us-central1:workhub-db
CLOUD_STORAGE_BUCKET=your-project-workhub-uploads
GCP_SERVICE_ACCOUNT_EMAIL=workhub-cloud-run-sa@your-project.iam.gserviceaccount.com

# =============================================================================
# Database Configuration (Cloud SQL)
# =============================================================================
DB_DIALECT=mssql
DB_NAME=workhub
DB_USER=sqlserver
DB_PASSWORD=<stored-in-secret-manager>  # Do not set here, use Secret Manager

# =============================================================================
# Application Secrets
# =============================================================================
SECRET_KEY=<stored-in-secret-manager>  # Do not set here, use Secret Manager
JWT_SECRET_KEY=<stored-in-secret-manager>  # Do not set here, use Secret Manager

# =============================================================================
# Storage Configuration
# =============================================================================
USE_CLOUD_STORAGE=true
UPLOAD_FOLDER=/tmp/uploads  # Fallback for local storage (not used when USE_CLOUD_STORAGE=true)

# =============================================================================
# Email/SMTP Configuration
# =============================================================================
EMAIL_NOTIFICATIONS_ENABLED=true
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=<stored-in-secret-manager>  # Do not set here, use Secret Manager
MAIL_DEFAULT_SENDER=noreply@yourdomain.com

# =============================================================================
# CORS and Frontend Configuration
# =============================================================================
ALLOWED_ORIGINS=https://workhub-frontend-xxxxxx.a.run.app,https://yourdomain.com
FRONTEND_URL=https://workhub-frontend-xxxxxx.a.run.app

# =============================================================================
# Cloud Run Specific
# =============================================================================
# These are set automatically by Cloud Run
# PORT=8080  # Cloud Run sets this automatically
# K_SERVICE=workhub-backend
# K_REVISION=workhub-backend-00001-xxx
# K_CONFIGURATION=workhub-backend

# =============================================================================
# GitHub Actions Secrets (to be set in GitHub repository settings)
# =============================================================================
# GCP_PROJECT_ID
# GCP_REGION
# GCP_SA_KEY (Service account JSON key)
# CLOUD_SQL_CONNECTION_NAME
# CLOUD_STORAGE_BUCKET
# GCP_SERVICE_ACCOUNT_EMAIL
# DB_USER
# BACKEND_URL (e.g., https://workhub-backend-xxxxxx.a.run.app)
# FRONTEND_URL (e.g., https://workhub-frontend-xxxxxx.a.run.app)
# ALLOWED_ORIGINS (comma-separated list)
# MAIL_SERVER
# MAIL_PORT
# MAIL_USERNAME
# MAIL_DEFAULT_SENDER

# =============================================================================
# Optional: Custom Domain Configuration
# =============================================================================
# CUSTOM_DOMAIN=workhub.yourdomain.com
# SSL_CERTIFICATE=managed  # GCP-managed certificate

# =============================================================================
# Logging and Monitoring
# =============================================================================
# LOG_LEVEL=INFO
# SENTRY_DSN=https://your-sentry-dsn (optional)

# =============================================================================
# Notes
# =============================================================================
# 1. Sensitive values (passwords, keys) should be stored in GCP Secret Manager
# 2. Reference secrets in Cloud Run using --set-secrets flag
# 3. Update ALLOWED_ORIGINS after deploying to include your actual URLs
# 4. For production, always use HTTPS URLs
# 5. Never commit actual credentials to version control

